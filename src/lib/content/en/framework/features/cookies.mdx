---
title: Cookies
description: Native cookie management in AzuraJS
category: Features
icon: lucide:cookie
order: 2
---

# Cookies 🍪

AzuraJS offers simple and efficient cookie management, allowing you to set, read, and delete cookies natively, without the need for external libraries. The `RequestServer` and `ResponseServer` objects are extended to facilitate this interaction.

## Reading Cookies

You can access cookies sent by the client via the `req.cookies` property or by using the `@Cookies()` decorator.

### Via `req.cookies`

```typescript
import { Controller, Get, Req, Res } from "azurajs/decorators";
import type { RequestServer, ResponseServer } from "azurajs/types";

@Controller("/auth")
export class AuthController {
  @Get("/check-session")
  checkSession(@Req() req: RequestServer, @Res() res: ResponseServer) {
    const sessionId = req.cookies.sessionId;
    if (sessionId) {
      return res.json({ authenticated: true, sessionId });
    } else {
      return res.status(401).json({ authenticated: false });
    }
  }
}
```

### Via `@Cookies()` Decorator

```typescript
import { Controller, Get, Cookies } from "azurajs/decorators";

@Controller("/auth")
export class AuthController {
  @Get("/check-session")
  checkSession(@Cookies("sessionId") sessionId?: string) {
    if (sessionId) {
      return { authenticated: true, sessionId };
    } else {
      return { authenticated: false };
    }
  }
}
```

## Setting Cookies

Use the `res.cookie()` method to set a cookie in the response. It accepts the name, value, and an options object to configure the cookie.

```typescript
import { Controller, Post, Res } from "azurajs/decorators";
import type { ResponseServer } from "azurajs/types";

@Controller("/auth")
export class AuthController {
  @Post("/login")
  login(@Res() res: ResponseServer) {
    const newSessionId = "some-unique-session-id";
    res.cookie("sessionId", newSessionId, {
      httpOnly: true, // Not accessible via JavaScript in the browser
      secure: process.env.NODE_ENV === "production", // Send only over HTTPS
      maxAge: 1000 * 60 * 60 * 24 * 7, // Expires in 7 days
      path: "/", // Available for the entire domain
      sameSite: "lax", // CSRF protection
    });
    return res.json({ message: "Login successful" });
  }
}
```

### Cookie Options

| Option | Type | Description |
| :--- | :--- | :--- |
| `maxAge` | `number` | Cookie lifespan in milliseconds. |
| `expires` | `Date` | Cookie expiration date. |
| `httpOnly` | `boolean` | If `true`, the cookie cannot be accessed via JavaScript. |
| `secure` | `boolean` | If `true`, the cookie is only sent over HTTPS requests. |
| `path` | `string` | URL path for which the cookie is valid. Default: `/`. |
| `domain` | `string` | Domain for which the cookie is valid. |
| `sameSite` | `"lax" \| "strict" \| "none"` | CSRF protection. |

## Deleting Cookies

To delete a cookie, use `res.clearCookie()`, passing the cookie name. Options should be the same as those used to set the cookie, except for `maxAge` and `expires`.

```typescript
import { Controller, Post, Res } from "azurajs/decorators";
import type { ResponseServer } from "azurajs/types";

@Controller("/auth")
export class AuthController {
  @Post("/logout")
  logout(@Res() res: ResponseServer) {
    res.clearCookie("sessionId", { path: "/" });
    return res.json({ message: "Logout successful" });
  }
}
```

## Next Steps 📖

- [🛡️ **Validation**](/docs/en/features/validation): Ensure cookie data is valid.
- [🌐 **CORS**](/docs/en/features/cors): Understand how cookies interact with CORS policies.
