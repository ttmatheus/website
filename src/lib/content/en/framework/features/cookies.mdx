---
title: Cookies
description: Native cookie management in AzuraJS
category: Features
icon: lucide:cookie
order: 2
---

# Cookies 🍪

AzuraJS provides straightforward and efficient cookie management, enabling you to set, read, and delete cookies natively without external dependencies. The `RequestServer` and `ResponseServer` objects are extended to facilitate these interactions.

## Reading Cookies

Cookies sent by the client can be accessed via the `req.cookies` property or injectively using the `@Cookies()` decorator.

### Via `req.cookies`

```typescript
import { Controller, Get, Req, Res } from "azurajs/decorators";
import type { RequestServer, ResponseServer } from "azurajs/types";

@Controller("/auth")
export class AuthController {
  @Get("/check-session")
  checkSession(@Req() req: RequestServer, @Res() res: ResponseServer) {
    const sessionId = req.cookies.sessionId;

    if (sessionId) {
      return res.json({ authenticated: true, sessionId });
    }

    return res.status(401).json({ authenticated: false });
  }
}
```

### Via `@Cookies()` Decorator

```typescript
import { Controller, Get, Cookies } from "azurajs/decorators";

@Controller("/auth")
export class AuthController {
  @Get("/check-session")
  checkSession(@Cookies("sessionId") sessionId?: string) {
    if (sessionId) {
      return { authenticated: true, sessionId };
    }

    return { authenticated: false };
  }
}
```

## Setting Cookies

Use the `res.cookie()` method to set a cookie in the outgoing response.

```typescript
import { Controller, Post, Res } from "azurajs/decorators";
import type { ResponseServer } from "azurajs/types";

@Controller("/auth")
export class AuthController {
  @Post("/login")
  login(@Res() res: ResponseServer) {
    const newSessionId = "unique-session-id";

    res.cookie("sessionId", newSessionId, {
      httpOnly: true,
      secure: process.env.NODE_ENV === "production",
      maxAge: 7 * 24 * 60 * 60 * 1000, // 7 days
      path: "/",
      sameSite: "lax",
    });

    return res.json({ message: "Login successful" });
  }
}
```

### Cookie Options

| Option     | Type                          | Description                                                    |
| :--------- | :---------------------------- | :------------------------------------------------------------- |
| `maxAge`   | `number`                      | Cookie lifespan in milliseconds.                               |
| `expires`  | `Date`                        | Absolute expiration date.                                      |
| `httpOnly` | `boolean`                     | Prevents client-side script access (recommended for security). |
| `secure`   | `boolean`                     | Sends cookie only over encrypted (HTTPS) connections.          |
| `path`     | `string`                      | URL path scope. Default is `/`.                                |
| `domain`   | `string`                      | Domain scope.                                                  |
| `sameSite` | `"lax" \| "strict" \| "none"` | Controls cross-site attribute for CSRF protection.             |

## Deleting Cookies

To remove a cookie, use `res.clearCookie()`. You must pass the same path and domain options used when setting the cookie to ensure successful deletion.

```typescript
import { Controller, Post, Res } from "azurajs/decorators";
import type { ResponseServer } from "azurajs/types";

@Controller("/auth")
export class AuthController {
  @Post("/logout")
  logout(@Res() res: ResponseServer) {
    res.clearCookie("sessionId", { path: "/" });
    return res.json({ message: "Logout successful" });
  }
}
```

## Next Steps 📖

- [🛡️ **Validation**](/docs/en/framework/features/validation): Ensure cookie data is valid.
- [🌐 **CORS**](/docs/en/framework/features/cors): Understand how cookies interact with CORS policies.
