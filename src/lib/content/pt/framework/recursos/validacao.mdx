---
title: Validação
description: Valide dados de requisição de forma elegante e type-safe
category: Recursos
icon: lucide:shield-check
order: 1
---

# Validação ✅

O AzuraJS inclui um sistema de validação robusto e integrado, projetado para garantir a integridade dos dados antes que alcancem sua lógica de negócios. Ele previne o processamento de dados inválidos e fornece inferência de tipos automática para usuários de TypeScript.

## O Azura Validator 🛡️

O validador oferece uma API fluida e encadeável inspirada no Zod, permitindo definições de esquema concisas e verificação rigorosa de dados.

### Definição de Schema

Defina schemas usando o utilitário `v` para criar regras de validação.

```typescript
import { v } from "azurajs/validators";

const CreateUserSchema = v.object({
  username: v.string().min(3).max(20),
  email: v.string().email(),
  age: v.number().int().min(18).optional(),
});

type CreateUserDto = v.infer<typeof CreateUserSchema>;
```

## Validando no Controller 🎯

Invoque o método `.safeParse()` do schema dentro do seu controller para validar o corpo das requisições recebidas.

```typescript
import { Controller, Post, Body, Res } from "azurajs/decorators";
import type { ResponseServer } from "azurajs/types";

@Post()
async createUser(@Body() body: unknown, @Res() res: ResponseServer) {
  const result = CreateUserSchema.safeParse(body);

  if (!result.success) {
    return res.status(400).json({
      error: "Validação falhou",
      details: result.error.format()
    });
  }

  const data = result.data;
  // Prossiga com execução segura e tipada
}
```

## Tipos de Dados Suportados 📊

O validador suporta um conjunto abrangente de tipos de dados e restrições:

- `v.string()`: Validação de string com helpers como `.email()`, `.url()`, `.uuid()`, `.min()`, `.max()`.
- `v.number()`: Validação numérica com helpers como `.int()`, `.positive()`, `.min()`, `.max()`.
- `v.boolean()`: Checa valores booleanos.
- `v.array(type)`: Valida arrays de um tipo específico.
- `v.object(shape)`: Valida estruturas de objeto aninhadas.
- `v.enum([values])`: Valida contra um conjunto de valores permitidos.

## Validação Automática 🏷️

Versões futuras do AzuraJS introduzirão validação baseada em decorators para integração ainda mais fluida diretamente nas assinaturas dos métodos.

## Próximos Passos 📖

- [🍪 **Cookies**](/docs/pt/framework/recursos/cookies): Gerenciamento nativo de cookies.
- [🚨 **Tratamento de Erros**](/docs/pt/framework/recursos/error-handling): Como lidar com falhas de validação.
