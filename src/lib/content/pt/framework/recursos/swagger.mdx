---
title: Swagger / OpenAPI
description: Gere documentação interativa para sua API automaticamente
category: Recursos
icon: lucide:book-open
order: 7
---

# Swagger / OpenAPI 📖

Uma documentação de API eficaz é essencial para a colaboração entre desenvolvedores. O AzuraJS simplifica isso com ferramentas nativas que geram automaticamente especificações OpenAPI 3.0 e servem uma interface visual interativa (Swagger UI).

## Configuração Rápida ⚡

Habilite o Swagger invocando `setupSwaggerWithControllers` no arquivo principal da sua aplicação.

```typescript
import { AzuraClient, setupSwaggerWithControllers } from "azurajs";
import { UserController } from "./controllers/UserController";

const app = new AzuraClient();

setupSwaggerWithControllers(
  app,
  {
    title: "Minha API Azura",
    version: "1.0.0",
    description: "Documentação automática das rotas",
    path: "/docs",
  },
  [UserController],
);

await app.listen(3000);
```

Acesse a documentação em `http://localhost:3000/docs`.

## Decorators do Swagger 🏷️

Enriqueça os detalhes da documentação usando decorators em controllers e métodos.

### `@ApiTags(...tags)`

Organiza endpoints em categorias.

```typescript
@ApiTags("Usuários")
@Controller("/users")
class UserController { ... }
```

### `@Swagger(config)`

Define sumários e descrições detalhadas para endpoints.

```typescript
@Swagger({
  summary: "Obter perfil",
  description: "Retorna dados privados do usuário autenticado"
})
@Get("/profile")
getProfile() { ... }
```

### `@ApiResponse(status, description, options?)`

Documenta possíveis retornos e schemas.

```typescript
@ApiResponse(200, "Sucesso", { type: UserSchema })
@ApiResponse(401, "Não autorizado")
@Get()
list() { ... }
```

## Especificação JSON 📄

O AzuraJS disponibiliza automaticamente o arquivo JSON bruto da especificação OpenAPI em `/api-spec.json`. Este endpoint pode ser usado para importar sua definição de API em ferramentas como Postman ou Insomnia, ou para geração de clientes.

## Próximos Passos 📖

- [🛡️ **Rate Limiting**](/docs/pt/framework/recursos/rate-limiting): Proteja seus endpoints documentados.
- [🌐 **CORS**](/docs/pt/framework/recursos/cors): Garanta que o Swagger UI funcione em diferentes domínios.
