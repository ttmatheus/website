---
title: CORS (Cross-Origin Resource Sharing)
description: Configure políticas de CORS para sua API AzuraJS
category: Recursos
icon: lucide:globe
order: 4
---

# CORS (Cross-Origin Resource Sharing) 🌐

O CORS é um mecanismo de segurança que permite ou restringe requisições de recursos vindas de domínios externos. O AzuraJS oferece um plugin de CORS flexível e pronto para produção, suportando configuração global e por rota.

## Configuração Global

Habilite e customize o CORS através do objeto `plugins` no `azura.config.ts`.

```typescript
// azura.config.ts
import type { ConfigTypes } from "azurajs/config";

const config: ConfigTypes = {
  plugins: {
    cors: {
      enabled: true,
      origins: ["http://localhost:3000", "https://seusite.com"],
      methods: ["GET", "POST", "PUT", "DELETE", "PATCH"],
      allowedHeaders: ["Content-Type", "Authorization"],
      exposedHeaders: ["X-Custom-Header"],
      credentials: true,
      maxAge: 3600,
    },
  },
};

export default config;
```

### Opções de `cors`

| Propriedade      | Tipo       | Descrição                                                                              |
| :--------------- | :--------- | :------------------------------------------------------------------------------------- |
| `enabled`        | `boolean`  | Ativa o middleware de CORS. Padrão: `false`.                                           |
| `origins`        | `string[]` | Lista de origens permitidas. Use `["*"]` para permitir todas (não seguro em produção). |
| `methods`        | `string[]` | Métodos HTTP permitidos. Padrão inclui verbos comuns.                                  |
| `allowedHeaders` | `string[]` | Cabeçalhos de requisição permitidos.                                                   |
| `exposedHeaders` | `string[]` | Cabeçalhos de resposta acessíveis ao navegador.                                        |
| `credentials`    | `boolean`  | Permite envio de cookies e headers de autorização entre origens. Padrão: `false`.      |
| `maxAge`         | `number`   | Duração do cache de preflight em segundos. Padrão: `3600`.                             |

## Requisições Preflight

O AzuraJS gerencia automaticamente requisições `OPTIONS` de preflight para operações CORS complexas, garantindo interação fluida com as políticas de segurança do navegador sem configuração de rota adicional.

## Próximos Passos 📖

- [🛡️ **Rate Limiting**](/docs/pt/framework/recursos/rate-limiting): Combine CORS com Rate Limiting para maior segurança.
- [🍪 **Cookies**](/docs/pt/framework/recursos/cookies): Entenda como `credentials: true` afeta o envio de cookies cross-origin.
