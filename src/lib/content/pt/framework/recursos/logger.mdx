---
title: Logger
description: Sistema de logging contextual e configurável no AzuraJS
category: Recursos
icon: lucide:clipboard-list
order: 3
---

# Logger 📊

O AzuraJS integra um sistema de logging contextual que se adapta ao ambiente de execução. Ele fornece informações detalhadas de debug durante o desenvolvimento e logs concisos e focados em performance em produção.

## Configuração do Logger

O comportamento do logger é controlado através do objeto `logging` no seu arquivo `azura.config.ts`.

```typescript
// azura.config.ts
import type { ConfigTypes } from "azurajs/config";

const config: ConfigTypes = {
  environment: "development",
  logging: {
    enabled: true,
    showDetails: true,
  },
};

export default config;
```

### Opções de `logging`

| Propriedade   | Tipo      | Padrão       | Descrição                                                                   |
| :------------ | :-------- | :----------- | :-------------------------------------------------------------------------- |
| `enabled`     | `boolean` | `true`       | Ativa ou desativa o middleware de logging.                                  |
| `showDetails` | `boolean` | `true` (dev) | Se ativado, inclui detalhes da requisição como parâmetros de query e corpo. |

## Logs de Requisição

Quando ativo, o sistema registra automaticamente requisições recebidas e respostas enviadas, capturando métodos HTTP, URLs, tempos de resposta e códigos de status.

### Exemplo de Saída (Desenvolvimento)

```bash
· 14:30:05  GET    /api/users
   › query: {}
   › params: {}
→ 200 (15ms) • IP:127.0.0.1
```

### Exemplo de Saída (Produção)

```bash
· 14:30:05  GET    /api/users • IP:192.168.1.100
→ 200 (15ms) • IP:192.168.1.100
```

## Usando o Logger Interno

Você pode utilizar a função `logger` nativa para criar entradas de log personalizadas dentro da lógica da sua aplicação. Estes logs integram-se diretamente ao sistema de logging do framework.

```typescript
import { logger } from "azurajs/utils/logger";

// Uso padrão
logger("info", "Usuário logado com sucesso!");
logger("warn", "Tentativa de acesso não autorizado.");
logger("error", "Erro ao processar dados do usuário.", { userId: 123 });
```

### Níveis de Log Suportados

- `"info"`
- `"warn"`
- `"error"`
- `"debug"`

## Próximos Passos 📖

- [🚨 **Tratamento de Erros**](/docs/pt/framework/recursos/error-handling): Veja como o logger pode ajudar a depurar erros.
- [⚙️ **Configuração**](/docs/pt/framework/comecando/configuracao): Entenda como o ambiente (`environment`) afeta o logging.
