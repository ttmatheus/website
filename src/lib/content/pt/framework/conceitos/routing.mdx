---
title: Routing
description: Aprenda sobre padrões de rota, parâmetros e correspondência no AzuraJS
category: Conceitos Principais
icon: lucide:route
order: 2
---

# Routing 🛣️

O sistema de roteamento do AzuraJS utiliza uma estrutura de dados **Radix Tree** (Árvore de Prefixos) de alta performance. Isso garante tempos de busca de rota constantes e extremamente rápidos, independentemente do número de rotas registradas na sua aplicação.

## Padrões de Rota 🧩

As rotas são definidas dentro dos métodos dos controllers usando decorators para especificar caminhos e parâmetros.

### Rotas Estáticas

Rotas estáticas mapeiam para caminhos fixos.

```typescript
@Get("/health")
check() { return "OK"; }
```

### Rotas Dinâmicas

Use o prefixo `:` para denotar parâmetros de rota dinâmicos.

```typescript
@Get("/users/:id")
getUser(@Param("id") id: string) {
  return { id };
}
```

### Múltiplos Parâmetros

Rotas podem manipular múltiplos segmentos dinâmicos.

```typescript
@Get("/org/:orgId/projects/:projectId")
getProject(@Param("orgId") org: string, @Param("projectId") project: string) {
  return { org, project };
}
```

## Métodos HTTP Suportados 📡

O AzuraJS fornece decorators para todos os métodos HTTP padrão:

- `@Get(path)`
- `@Post(path)`
- `@Put(path)`
- `@Delete(path)`
- `@Patch(path)`
- `@Head(path)`
- `@Options(path)`

## Prioridade de Rota 🔢

O algoritmo de roteamento aplica regras de precedência específicas para determinar qual rota corresponde a uma requisição:

1.  **Rotas Estáticas**: Correspondências exatas como `/users/profile` têm prioridade sobre padrões dinâmicos.
2.  **Rotas Parametrizadas**: Padrões como `/users/:id` são avaliados se nenhuma correspondência estática for encontrada.
3.  **Wildcards**: Rotas "catch-all" são verificadas por último (se aplicável).

## Case Sensitivity 🔠

Por padrão, a correspondência de rotas no AzuraJS é **case-sensitive**. Esse comportamento garante controle preciso sobre as estruturas de URL, embora possa ser ajustado via configuração global (`azura.config.ts`).

## Próximos Passos 📖

- [🎯 **Controllers**](/docs/pt/framework/conceitos/controllers): Como organizar suas rotas.
- [🔌 **Middleware**](/docs/pt/framework/conceitos/middleware): Intercepte requisições antes que cheguem às rotas.
