---
title: Controllers
description: Aprenda sobre o padrão controller no AzuraJS
category: Conceitos Principais
icon: lucide:target
order: 1
---

# Controllers 🎯

Os controllers servem como o núcleo arquitetural das aplicações AzuraJS. Eles utilizam o padrão MVC (Model-View-Controller) para organizar endpoints de API em classes lógicas, sustentáveis e seguras com tipos, utilizando decorators TypeScript.

<Callout type="info" title="Separação de Responsabilidades">
  Controllers promovem uma arquitetura limpa, separando a lógica de roteamento
  da lógica de negócios. Essa organização reduz a duplicação de código e melhora
  a manutenibilidade.
</Callout>

## O que é um Controller? 📚

Um controller é uma classe TypeScript decorada com `@Controller`. Ela agrupa manipuladores de rota relacionados. Cada método dentro da classe representa um endpoint específico e é decorado com decorators de métodos HTTP como `@Get`, `@Post`, `@Put` ou `@Delete`.

```typescript
import { Controller, Get, Post } from "azurajs/decorators";

@Controller("/api/products")
export class ProductController {
  @Get()
  list() {
    return { products: [] };
  }

  @Post()
  create() {
    return { message: "Product created" };
  }
}
```

## Decorator Controller 🏷️

O decorator `@Controller(prefix?)` define a rota base para todos os métodos dentro da classe.

- **Uso Básico**: `@Controller("/users")` prefixa `/users` em todas as rotas aninhadas.
- **Sem Prefixo**: `@Controller()` significa que as rotas serão definidas apenas pelos decorators de seus métodos.
- **Caminhos Aninhados**: `@Controller("/api/v1/admin")` permite aninhamento profundo de caminhos.

## Injeção de Parâmetros 💉

O AzuraJS fornece um conjunto dedecorators para injetar dados da requisição diretamente nos métodos do seu controller.

| Decorator        | Descrição                                              |
| :--------------- | :----------------------------------------------------- |
| `@Req()`         | Injeta o objeto `RequestServer` completo.              |
| `@Res()`         | Injeta o objeto `ResponseServer` completo.             |
| `@Body()`        | Injeta o corpo da requisição processado (parsed body). |
| `@Param(name)`   | Injeta um parâmetro específico da URL (ex: `:id`).     |
| `@Query(name)`   | Injeta um parâmetro específico da query string.        |
| `@Headers(name)` | Injeta um valor específico do cabeçalho.               |
| `@Cookies(name)` | Injeta um valor específico de cookie.                  |
| `@Ip()`          | Injeta o endereço IP do cliente.                       |

## Melhores Práticas ✨

1.  **Responsabilidade Única**: Dedique cada controller a um único recurso ou domínio (ex: `UserController`, `OrderController`).
2.  **Camada de Serviço**: Delegue lógica de negócios complexa para classes de serviço; mantenha os controllers focados na manipulação HTTP.
3.  **Respostas Consistentes**: Mantenha uma estrutura de resposta JSON padronizada em toda a sua API.
4.  **Tratamento de Erros**: Utilize a classe `HttpError` para relatórios de erro consistentes com códigos de status apropriados.

## Próximos Passos 📖

- [🛣️ **Routing**](/docs/pt/framework/conceitos/routing): Aprenda sobre padrões de rota e parâmetros.
- [🏷️ **Decorators**](/docs/pt/framework/conceitos/decorators): Domine todos os decorators disponíveis.
- [🛡️ **Validação**](/docs/pt/framework/recursos/validacao): Valide dados de requisição automaticamente.
