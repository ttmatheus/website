---
title: Uso com JavaScript
description: Como usar o AzuraJS em projetos JavaScript puro
category: Começando
icon: lucide:code
order: 4
---

# Uso com JavaScript ☕

O AzuraJS foi projetado com uma abordagem **TypeScript-first**, aproveitando decorators para uma API expressiva. No entanto, ele é totalmente compatível com ambientes **JavaScript puro**. No JavaScript, você utilizará a API funcional fornecida pela instância `AzuraClient` em vez de decorators.

## Configuração do Projeto

Projetos JavaScript utilizam o `azura.config.js` para configuração. A configuração é direta, pois não requer etapas de compilação TypeScript.

**azura.config.js**

```javascript
const config = {
  environment: "development",
  server: {
    port: 3000,
  },
  logging: {
    enabled: true,
  },
};

export default config;
```

## Criando Rotas Funcionais

Ao usar JavaScript, as rotas são definidas diretamente na instância `app` usando métodos como `.get()`, `.post()`, `.put()` e `.delete()`.

**src/index.js**

```javascript
import { AzuraClient } from "azurajs";

const app = new AzuraClient();

app.get("/", (req, res) => {
  res.send("Olá do AzuraJS em JavaScript!");
});

app.post("/data", async (req, res) => {
  const body = await req.json();
  res.json({ received: body, status: "success" });
});

app.listen().then(() => {
  console.log("🚀 Servidor AzuraJS JS rodando em http://localhost:3000");
});
```

## Acessando Dados da Requisição

Os manipuladores de rota recebem os objetos `req` e `res`, que correspondem a `RequestServer` e `ResponseServer`. Estes objetos fornecem acesso direto aos dados da requisição, como parâmetros, query strings e headers.

```javascript
app.get("/users/:id", (req, res) => {
  const userId = req.params.id;
  const queryParam = req.query.search;
  const userAgent = req.headers["user-agent"];

  res.json({
    id: userId,
    search: queryParam,
    userAgent: userAgent,
  });
});
```

## Próximos Passos 📖

- [⚙️ **Configuração**](/docs/pt/framework/comecando/configuracao): Entenda todas as opções de configuração do AzuraJS.
- [🛣️ **Routing**](/docs/pt/framework/conceitos/routing): Explore as capacidades de roteamento detalhadamente.
