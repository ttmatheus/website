---
title: Instalação
description: Instale AzuraJS no seu projeto em minutos
category: Começando
icon: lucide:package
order: 2
---

# Instalação 📦

Comece a usar o AzuraJS instalando-o em seu projeto **Node.js** ou **Bun**. Este guia irá orientá-lo através do processo de configuração.

## Pré-requisitos ✅

Certifique-se de que seu ambiente atende aos seguintes requisitos:

<Callout type="warning" title="Requisitos de Sistema">
  - **Node.js** 18.0.0+ ou **Bun** 1.0.0+ - **TypeScript** 5.0+ (Obrigatório
  para suporte a decorators) - Um gerenciador de pacotes: **npm**, **yarn**,
  **pnpm** ou **bun**
</Callout>

## Instalação do Pacote 📥

Instale o pacote `azurajs` utilizando seu gerenciador de pacotes preferido:

<Tabs items={["npm", "pnpm", "yarn", "bun"]}>
  <Tab value="npm">```bash npm install azurajs ```</Tab>
  <Tab value="pnpm">```bash pnpm add azurajs ```</Tab>
  <Tab value="yarn">```bash yarn add azurajs ```</Tab>
  <Tab value="bun">```bash bun add azurajs ```</Tab>
</Tabs>

## Configuração do TypeScript 🛠️

O AzuraJS utiliza **decorators** para fornecer sua sintaxe expressiva. Você deve habilitá-los no seu arquivo `tsconfig.json`.

Adicione ou atualize as seguintes opções do compilador:

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

## Estrutura Recomendada 📁

Para um código escalável e de fácil manutenção, recomendamos a seguinte estrutura de diretórios:

```text
my-azura-api/
├── src/
│   ├── controllers/      # Controllers de rotas e manipulação de requisições
│   ├── services/         # Lógica de negócios e acesso a dados
│   ├── middlewares/      # Funções de middleware personalizadas
│   └── index.ts          # Ponto de entrada da aplicação
├── azura.config.ts       # Configuração do framework
├── tsconfig.json         # Configuração do TypeScript
└── package.json          # Dependências do projeto
```

## Próximos Passos 🚀

Com o AzuraJS instalado, você está pronto para construir. Siga para o guia de [Início Rápido](/docs/pt/framework/comecando/inicio-rapido) para criar sua primeira API.
