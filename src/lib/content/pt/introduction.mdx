---
title: Bem-vindo ao AzuraJS
description: Um framework web moderno, rápido e focado em TypeScript para Node.js e Bun
category: Começando
icon: lucide:sparkles
status: new
date: 2026-02-01
order: 1
---

# Bem-vindo ao AzuraJS ⚡

AzuraJS é um framework web moderno e de alta performance, projetado para ambientes **Node.js** e **Bun**. Ele utiliza o poder dos **decorators do TypeScript** para oferecer uma experiência de desenvolvimento limpa, intuitiva e robusta na construção de aplicações backend escaláveis.

<Callout type="info" title="Ideal para Desenvolvimento Moderno">
  AzuraJS é construído para desenvolvedores que exigem velocidade, segurança de
  tipos e simplicidade, sem a sobrecarga de frameworks complexos e pesados.
</Callout>

## ✨ Por que Escolher AzuraJS?

| Recurso                     | Descrição                                                                                                                        |
| :-------------------------- | :------------------------------------------------------------------------------------------------------------------------------- |
| **Nativo em TypeScript**    | Desenvolvido com TypeScript desde o início, oferecendo segurança de tipos impecável e suporte superior ao IntelliSense.          |
| **Baseado em Decorators**   | Utilize decorators modernos para definir rotas, middlewares e validações, resultando em uma estrutura de código limpa e legível. |
| **Alta Performance**        | Otimizado para velocidade máxima com overhead mínimo e zero dependências externas desnecessárias.                                |
| **Leve**                    | Um núcleo minimalista com apenas as dependências essenciais, evitando o inchaço do `node_modules`.                               |
| **Compatibilidade Express** | Totalmente compatível com o vasto ecossistema de middlewares e plugins do Express.                                               |
| **Configuração Flexível**   | Suporte para configuração via TypeScript, JSON ou YAML, adaptando-se ao seu fluxo de trabalho.                                   |

## 🎨 Recursos Principais

AzuraJS fornece um conjunto abrangente de ferramentas para o desenvolvimento profissional de APIs:

- **Injeção de Parâmetros Inteligente**: Utilize decorators como `@Body`, `@Query`, `@Param`, `@Req`, `@Res` para acesso explícito aos dados.
- **Validação Integrada**: Sistema robusto de validação de DTOs e esquemas com total inferência de tipos.
- **Gerenciamento de Cookies**: Suporte nativo para análise e serialização segura de cookies.
- **CORS Pronto para Produção**: Plugin CORS configurável, projetado para segurança e flexibilidade.
- **Rate Limiting**: Proteção integrada contra abusos e ataques DDoS com configuração por rota.
- **Cluster & Proxy**: Suporte nativo a cluster para performance multi-core e gerenciamento de proxies confiáveis.
- **Logging Contextual**: Sistema de logging adaptável para monitoramento e depuração eficazes.
- **Tratamento de Erros Estruturado**: Gerenciamento centralizado de erros com `HttpError` e middleware dedicado.

## 🚀 Simples e Poderoso

Crie uma API CRUD totalmente funcional com o mínimo de código boilerplate. O exemplo a seguir demonstra a implementação completa de um controller:

```typescript
import { AzuraClient } from "azurajs";
import {
  applyDecorators,
  Controller,
  Get,
  Post,
  Body,
  Param,
} from "azurajs/decorators";

@Controller("/api/users")
class UserController {
  @Get()
  getAllUsers() {
    return { users: ["Alice", "Bob", "Charlie"] };
  }

  @Get("/:id")
  getUser(@Param("id") id: string) {
    return { id, name: `Usuário ${id}` };
  }

  @Post()
  createUser(@Body() data: unknown) {
    return { id: Date.now(), ...data };
  }
}

const app = new AzuraClient();
applyDecorators(app, [UserController]);
await app.listen();
```

## 📚 Comece Agora

Inicie sua jornada com os seguintes recursos:

- [📦 **Instalação**](/docs/pt/framework/comecando/instalacao): Configure seu ambiente e instale o AzuraJS.
- [🚀 **Início Rápido**](/docs/pt/framework/comecando/inicio-rapido): Crie sua primeira API REST em minutos.
- [⚙️ **Configuração**](/docs/pt/framework/comecando/configuracao): Aprenda sobre as opções de configuração do servidor.
- [🎯 **Controllers**](/docs/pt/framework/conceitos/controllers): Entenda como estruturar a lógica da sua aplicação.
